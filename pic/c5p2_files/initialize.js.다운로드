(function () {
  // eslint-disable-next-line no-undef
  Kakao.init('2541680cdc2a1442dd7f98bb3f8446a8');

  var userAgent = navigator.userAgent;
  // IE 하위버전 확인
  var isDownIEVersion = /MSIE ([0-9]{1,})/.test(userAgent);

  // 브라우저 호환 유도 페이지 이동
  if (isDownIEVersion) {
    window.location.href = 'http://commevent.interpark.com/browserUpgrade';
  }
  // document.domain = 'interpark.com';

  // eslint-disable-next-line no-undef
  // var isDesktop = $('body').hasClass('desktop');
  //
  // window.dataLayer = window.dataLayer || [];
  // function gtag() {
  //   // eslint-disable-next-line no-undef
  //   dataLayer.push(arguments);
  // }
  //
  // gtag('js', new Date());
  // if (isDesktop) {
  //   gtag('config', 'UA-61765461-1');
  // } else {
  //   gtag('config', 'UA-30729705-1');
  // }
})();
